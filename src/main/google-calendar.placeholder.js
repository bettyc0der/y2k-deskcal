/**
 * Phase 2 placeholder.
 * Google Calendar sync is intentionally not wired in yet.
 * Keep .env, credentials.json, and token.json OUT of GitHub.
 */
async function syncGoogleCalendarPlaceholder(){ return { ok:false, message:'Google Calendar sync is not enabled yet.' }; }
module.exports={ syncGoogleCalendarPlaceholder };
